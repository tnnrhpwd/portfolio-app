{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "description": "Maps local secret files (relative to repo root) to their encrypted filenames in the portfolio-app-secrets backup repo. Add an entry here whenever a new .env file needs to be backed up.",
  "files": [
    { "source": "backend/.env", "encrypted": "backend.env.age" }
  ]
}
