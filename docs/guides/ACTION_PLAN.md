# Action Plan (Single Source of Truth)

This is the one doc to work from going forward. It consolidates the reasoning from [BUSINESS_PLAN.md](./BUSINESS_PLAN.md) (what we're actually selling) and [ETHICAL_MONETIZATION_STRATEGIES.md](./ETHICAL_MONETIZATION_STRATEGIES.md) (how to monetize it honestly) into a concrete, ordered list of next steps. Those two docs remain as background/rationale — this doc is what you actually execute against.

## The premise, restated in one place

Simple is an AI agent that perceives what a user does on their Windows PC and acts on their behalf, described in plain English instead of scripts. AI chat and cloud storage support that core loop. The Free tier's job is to let someone try it and see for themselves whether it's useful; Pro removes the daily ceiling once someone has already felt real value. Every action below either (a) fixes/validates that the core product actually works, (b) states plainly what the product does without oversell, (c) makes the payment moment honestly reflect what's being bought, or (d) makes the free→paid conversion more natural — none of it is about pushing harder on marketing.

## How to use this document

This is a **living checklist, not a one-time script.** A large part of it — especially Phase 0 — depends directly on the addon's real implementation state, which changes as you work on it. Treat it accordingly:
- Check items off (`- [x]`) as they're actually done, and add new checklist items as new gaps are found — don't let the doc drift out of sync with reality the way the old pricing docs did.
- Phase 0 (below) should be revisited and re-edited every time you make real progress on the addon — add newly-discovered broken features, remove ones that are now fixed, and be specific (which feature, what fails, roughly why) rather than leaving stale generic entries.
- Phases 1, 3, and 5 (copy, payment page, funnel) are largely **blocked by Phase 0** in spirit — there's limited point polishing descriptions or pricing for features that don't work yet. Feel free to do small, low-effort parts of them opportunistically, but don't treat them as urgent until Phase 0 items are actually resolved.

## A note on tone

This plan deliberately avoids marketing-speak, urgency, and persuasion tactics. The intent is **word of mouth, not advertising**: people who try it and find it genuinely useful will tell others. The site's job is to describe plainly and honestly what the product does, get out of the way, and let the product itself do the convincing. Concretely, that means:
- Prefer plain, factual descriptions over hype adjectives ("does X" instead of "revolutionary," "powerful," "game-changing").
- No fabricated urgency, no growth-hacking prompts, no aggressive upsell nudges.
- It's fine — good, even — if the homepage stays understated. The goal of copy changes below is *clarity*, not *persuasion*.

---

## Phase 0 — Fix the actually-broken core, and don't sell it until it works

This is the real top priority, ahead of everything else in this document. As of this writing, key parts of the core perceive→act loop are reported broken, not just unreliable:
- **Key simulation** — many parts of simulated keyboard/mouse input are not functional.
- **Perception loop (action perception)** — totally broken.
- **Auto action execution** — totally broken.

There's no honest way to describe, price, or word-of-mouth-market a product whose core loop doesn't work. Every other phase in this doc assumes a working core to be honest *about* — so until Phase 0 is resolved, treat Phases 1/3/5 as optional/opportunistic, not urgent.

### Update this section as you go

This list will be wrong the moment it's written, on purpose — it should track today's real state, not a fixed plan. Each time you fix, partially fix, or discover a new broken piece:
- Move fixed items to a `Fixed` note (or just delete the line) instead of leaving stale checkboxes.
- Add newly-discovered gaps as their own line, naming the specific feature/file and what breaks, not a vague restatement.
- Don't round up — if something "mostly" works, say what fails and when, not just "done."

- [ ] **Key simulation**: identify why simulated key/mouse input fails (relevant code: [action-service.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/action-service.js), [action-bridge.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/action-bridge.js), [automation/tools/input.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/tools/input.js)) — get basic keystrokes and clicks working reliably before anything downstream that depends on them.
  - **Found and partially fixed a concrete bug**: `mouse_drag`/`mouse_path`/`click_at` moved the cursor via `SetCursorPos`, which was confirmed (via a new live eval scenario, [21-mouse-drag-window-titlebar.json](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/eval/scenarios/21-mouse-drag-window-titlebar.json)) to silently no-op in at least one tested environment — the tool reported `ok: true` while the cursor never actually moved, so every drag/click-and-hold landed at a single frozen point instead of moving. Replaced the cursor-move primitive with `SendInput`+`MOUSEEVENTF_ABSOLUTE` (plus a `SetCursorPos` fallback and a `GetCursorPos` verify) in `input.js`. After the fix, direct `SendInput` cursor movement was confirmed pixel-accurate in that same environment, but a real click-and-drag on a window title bar still only moved the window partially in one run (dx=90/dy=91 against a 200/200 target on a modern WinUI Notepad title bar; a classic Win32 title bar drag under the same test environment moved 0/0) — so click-and-hold reliability is improved but **not yet confirmed fully fixed**. This needs re-testing on a real desktop session (not the constrained/remote session used to first diagnose it) via `npm run eval:live -- server/automation/eval/scenarios/21-mouse-drag-window-titlebar.json` from `simple-addon/`, since the remaining shortfall may be session/compositor-specific (foreground-lock, remote-desktop cursor handling) rather than a code bug.
  - **Test coverage gap closed**: previously *nothing* in `npm test`/`npm run eval` exercised `mouse_drag` or `input_hold` at all — a fully green run gave zero signal about click-and-hold. The new live scenario above closes that gap for drag; `input_hold` (held keys/buttons) still has no equivalent real-desktop regression test and should get one next.
  - **Update (2026-08-20 status check):** several more real-world click/window bugs were found and fixed since the above (Aug 8–13), all discovered via actual dogfooding rather than the eval scenarios: `click_at` silently dropped a `modifiers` (shift/ctrl/alt/win) param compiled macros could request (e.g. Minecraft's shift-click inventory transfer), so modifier-clicks landed as plain left-clicks — fixed in `tools/input.js`/`tools/skill.js`. `window_focus`/`window_list`/`window_set_rect` were missing File Explorer windows entirely, and window-focus was un-maximizing a window just to focus it (visibly disruptive). A Minecraft-specific unpause/escape input loop bug was also fixed. These are genuine reliability improvements to the same code paths this item is about, but **none of them have been run through `validate-core-functionality.js`** — see the note below.
  - **First real measurement (2026-08-20, single run, `npm run validate:core`, log: `eval/live-results/1787277577920.json`):** confirms this is still broken, not just unmeasured. Scenario 1 (type+save in Notepad) failed — no file was ever created, meaning the save dialog flow doesn't reliably complete. Scenario 3 (click-to-select then F2-rename in File Explorer) failed — couldn't find the file item in Explorer's UIA tree, i.e. the click-to-select step itself didn't register. Scenario 9 (drag-and-drop with a moving mouse path) failed the same way — couldn't find the source file item at all. Scenario 10 (held-input safety on focus loss) failed with an oddly bare `window_focus failed: found` error, which itself looks like a bug in the error-message plumbing (swallowing the real underlying error), not just a flaky scenario. Only scenario 4 (Alt-Tab between two windows) passed. **This is a single run, not the 10-run reliability tally the gate below requires — but the hard gate ("each of these works at all") is not yet met**, since three of four testable key/mouse scenarios failed outright.
  - **Update (2026-08-21 fix session): scenarios 1, 3, 9, 10 all now pass individually and together.** Root causes found and fixed, all in `simple-addon/server/automation/tools/` and `eval/validate-core-functionality.js`:
    - **Scenario 1 (type+save)**: two stacked bugs. (a) `text_type`'s SendKeys path built its PowerShell string literal via `JSON.stringify()`, which produces JSON (backslash-escaped) syntax — but PowerShell double-quoted strings don't treat `\` as an escape character, so every JSON-escaped `\\` survived as a literal double backslash once PowerShell parsed it, turning any typed Windows path (e.g. the Save-As filename) into an invalid one. Replaced with a proper single-quoted-PowerShell-literal helper (`_psQuote`) in `text-type.js`. (b) Both `input.js`'s `Focus-WindowByTitle` and `text-type.js`'s `Focus-Title` resolved windows via `Get-Process`'s `MainWindowHandle`, which can never resolve to a dialog a process opens afterward (Notepad's own Ctrl+S "Save As" dialog is a separate top-level HWND, same pid, not a new process) — so a `focusWindowTitle` aimed at "Save" silently focused nothing, and the filename landed back in the document instead of the dialog. Rewrote both to walk every real top-level window via `EnumWindows` (same technique `system.js`'s `window_focus` already used), which sees dialogs too. Also discovered (and worked around in the eval scenario) that modern packaged Notepad restores its previous tab/file session on every relaunch — including tabs pointing at a since-deleted sandbox file, which pops a blocking "Cannot find the file" modal — so `cleanDesktop()` now clears Notepad's own `LocalState\TabState`/`WindowState` files as part of test cleanup, and scenario 1 sends Ctrl+N first to guarantee a truly blank tab regardless.
    - **Scenarios 3 & 9 (Explorer select/rename, drag-and-drop)**: shared root cause, exactly as suspected. `uia.js`'s `uia_find`/`uia_invoke`/`uia_get_text` used `runPsJson` (pipes the script over stdin) instead of `runPsJsonFile` (writes to a temp `.ps1` and runs it) — `ps-runner.js`'s own doc comment already warned multi-line function+`Add-Type` scripts don't survive the stdin transport, and confirmed empirically it silently returned `null` every time, meaning `uia_find` never actually found anything, ever. Switched all three to `runPsJsonFile` (matching `uia_snapshot`, which already used it). Scenario 9 additionally had its own bug once file-finding worked: the source/destination Explorer windows opened directly on top of each other, and re-focusing the source window right before the drag brought it on top of the drop-point coordinate too — tiled both windows into non-overlapping halves via `window_set_rect` before computing coordinates.
    - **Scenario 10 (held-input focus-loss safety)**: two bugs, per the task's own instructions to fix the message plumbing first. (a) `parseCliXmlError` in `system.js` naively split PowerShell's CLIXML error text on `\r\n` and grabbed "the line before `+ CategoryInfo`" — but CLIXML also uses `\r\n` purely to line-wrap long `<S S="Error">` chunks for readability, unrelated to real line breaks, and one such wrap happened to land exactly between "not" and "found" in `Write-Error 'window not found'`, truncating the reported message to a bare `"found"`. Fixed to locate the CategoryInfo boilerplate directly in the un-split text and flatten everything before it into one logical line before extracting the message. (b) With the real message visible (`window not found`), the actual bug: `window_focus({processName:'calculatorapp'})` genuinely couldn't find Calculator, for two reasons — modern WinUI3 apps (Notepad, Calculator, etc.) spawn small auxiliary top-level "PopupHost" windows (class `Xaml_WindowedPopupClass` / `Microsoft.UI.Content.PopupWindowSiteBridge`) that `Get-CandidateWindows`'s `EnumWindows` walk could match ahead of the real window; and Calculator's actual top-level window's owning thread belongs to `ApplicationFrameHost.exe` (the classic-UWP frame host), which was on the hard shell-host denylist, so the window got filtered out entirely before any processName match even ran. Fixed both in `system.js`: filter out literal `"PopupHost"`-titled windows, and resolve ApplicationFrameHost-owned windows to their actual hosted app's process (via `EnumChildWindows`) instead of denylisting them outright.
  - **Update (2026-08-21): investigated the scenario 6 "112s hang, `input_tap failed: powershell exited with null`" timing report.** Root cause: `input.js`/`system.js`/`ps-runner.js`'s PowerShell runners only ever killed the immediate child process on timeout (`child.kill('SIGKILL')`) — if a grandchild is still running at that moment (e.g. `csc.exe` compiling the `Add-Type` C# shim on a cold cache) and inherits the child's stdout/stderr pipes, Node's `close` event doesn't fire until that grandchild exits on its own, silently turning an intended ~5s timeout into an much longer real hang, with a useless `powershell exited with null` message once it finally did close (`null` = killed by signal, not a real exit code). Fixed by killing the whole process tree via `taskkill /PID <pid> /T /F` on timeout, and reporting a clear `timed out after <n>ms (process killed)` message instead. Applied to all four PowerShell runner implementations (`input.js`, `system.js`, `text-type.js`, `ps-runner.js`) for consistency. Scenario 6 itself turned out to have a second, unrelated bug once this and the scenarios-3/9 `uia.js` fix landed: it targeted a **"Format" menu that no longer exists** in the currently-installed Notepad version (Word Wrap moved to **View**, and its accessible name is lowercase `"Word wrap"`, not `"Word Wrap"`), and sent Alt and the mnemonic letter as two separate `input_tap` calls, which this WinUI MenuBar's accelerator handling doesn't reliably honor (unlike a classic Win32 menu) — a stray `"v"` was observed typed literally into the document. Fixed by opening the menu with a single held-Alt-plus-letter combo (matching scenario 4's existing Alt+Tab pattern) and correcting the menu/name. **Scenario 6 now passes reliably.**
  - **Update (2026-08-21): scenario 7 (record → compile → auto-replay) now passes.** Root cause: `PollingInputSource` (`recorder/polling-source.js`) samples keyboard state every `pollMs` (50ms default) via `GetAsyncKeyState` — but `text_type`'s SendKeys strategy injects each character's keydown/keyup as a sub-millisecond pulse regardless of any delay *between* characters, far too brief for a 15–50ms poll to reliably catch, so the recording captured no typed-text keystrokes at all (only the window-title changes Notepad happens to show for unsaved documents were visible). Reduced the default poll interval to 15ms (a real, generally-useful improvement, though it doesn't fully solve the underlying pulse-vs-poll mismatch — see the file's own doc comment on why a native low-level hook is the actual long-term fix) and, in the eval scenario itself, replaced the SendKeys-based "simulate a human typing" step with real held-key `input_tap` calls (genuine OS-level dwell time per character, same as an actual keystroke), which the poller can always catch regardless of interval. Also found and fixed: the Save-As dialog's edit control can take longer to actually start accepting keystrokes than the window itself takes to report as foreground (observed: the first several characters of a path typed right after the dialog appeared were silently dropped, not corrupted/reordered — everything typed after a couple of seconds landed fine) — bumped `Set-ForegroundWindowForce`'s post-switch settle delay in `input.js` from 150ms to 400ms.
  - **Verification**: scenarios 1, 2, 3, 4, 6, 9, 10 all pass individually and together in a combined run (log: `eval/live-results/1787354124273.json`); scenario 7 passes on its own (it's slow — ~3.5 minutes — because of the real-keystroke-dwell approach above, but reliable). `npm test`/`npm run eval` (the separate, CI-safe suite) still fully green. **This is still only spot-checks across a handful of runs today, not the 10-run/multi-day reliability tally the gate below requires** — see the updated readiness-bar note below for what's still outstanding.
- [ ] **Perception loop**: get action perception (detecting what the user actually did) working end-to-end for at least one simple, well-defined case, before claiming it handles the general "perceives what you do" case anywhere in copy.
- [ ] **Auto action execution**: get automatic playback/execution of a captured action sequence working reliably for that same simple case.
  - **Update (2026-08-20 status check):** the Recorder & Skills dashboard tab was substantially redesigned (Aug 8–13) with a natural-language macro compiler and a live step-execution console (see [nl-compiler.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/nl-compiler.js)), which is real forward progress on record→replay (scenario 7) and planner-driven execution (scenario 8). Still unchecked here because it hasn't been validated against the pass/fail bar those scenarios define — "the UI got nicer" isn't the same claim as "the loop is reliable."
  - **First real measurement (2026-08-20, single run):** scenario 6 (read a UI value back via perception) failed after a long hang (112s) with `input_tap failed: powershell exited with null` — the click/keystroke step feeding into the perception check itself broke before perception was even exercised. Scenario 7 (record → compile → auto-replay) failed because the recording phase never produced the expected file — record→replay is broken at the capture step, before compilation or replay are even reached. Scenario 8 (planner-driven natural-language execution) was **skipped, not failed** — it needs a signed-in JWT (all LLM calls now proxy through the backend per the Bedrock migration above) and none was resolvable in this environment; re-run after signing in via the web app once to get a real pass/fail on this one. Scenario 5 (perceive human-typed input) was also skipped (needs `--interactive` and a human at the keyboard) — still untested either way.
  - **Update (2026-08-21): scenarios 6 and 7 now both pass** — see the detailed root-cause notes under "Key simulation" above (PowerShell-runner timeout/process-tree handling + a stale "Format" menu reference for scenario 6; `PollingInputSource`'s poll-vs-SendKeys-pulse mismatch + a Save-As dialog settle-timing bug for scenario 7). Scenarios 5 and 8 are still only skipped, not actually validated — scenario 5 needs a real `--interactive` run with a human typing, and scenario 8 needs a signed-in JWT in this environment; neither has happened yet.
- [x] **Workspace Profiles (new feature)**: added the ability to capture the current window arrangement (running apps, per-window position/size, and minimized/maximized/normal state) as a named profile and restore it on demand. Tray → right-click → **Workspace** → **Save New…** prompts for a name via a small modal ([workspace-prompt.html](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/workspace-prompt.html)) and captures every visible window; saved profiles then appear as their own submenu entries (Restore/Delete) under **Workspace**. Implementation: capture/apply primitives as new automation tools `window_snapshot`/`window_set_rect` in [system.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/tools/system.js); storage + restore orchestration (matches already-running windows first, relaunches missing ones via `open_app`, otherwise reports them skipped) in [workspace-profiles.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/workspace-profiles.js); HTTP API + tool registration in [automation/index.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/index.js); tray submenu in [tray.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/tray.js) and wiring in [main.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/main.js). Manually verified end-to-end (save → move a window → restore → window returns to its saved position/size) on a real desktop session; now covered by a new automated eval scenario — [validate-core-functionality.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/eval/validate-core-functionality.js) scenario 11 (capture → move → restore via `window_snapshot`/`window_set_rect`), added 2026-09-04. Not yet run on a real desktop session — needs the same live run as the other scenarios above before it counts toward the reliability tally.
- [ ] **First readiness-bar measurement taken 2026-08-20 — result: 1 pass / 7 fail / 2 skip out of 10, on a single run.** Ran `npm run validate:core` for the first time ever (log: `simple-addon/server/automation/eval/live-results/1787277577920.json`). Only scenario 4 (Alt-Tab focus switch) passed. Seven scenarios failed outright (1, 2, 3, 6, 7, 9, 10 — see the per-item notes above for exact errors). Two were skipped, not failed, for expected reasons (scenario 5 needs `--interactive` + a human typing; scenario 8 needs a signed-in JWT, which wasn't available in this environment). **This is real evidence, not a feeling, and it confirms the doc's suspicion**: key/mouse simulation and record→replay are still broken in ordinary, common-app cases, not just edge cases — the hard gate ("each of these works at all") is not met. Next steps: (1) fix the concrete failures above one at a time, re-running `--only=<n>` after each fix; (2) sign into the web app once and re-run so scenario 8 gets a real pass/fail instead of a skip; (3) once scenarios are passing individually, start accumulating the 10-run/multi-day reliability tally the gate below actually requires — this single run is nowhere near sufficient for that.
- [ ] **Update (2026-08-21) — all 8 non-skipped scenarios now pass individually, including together in a combined run.** Scenarios 1, 2, 3, 6, 7, 9, 10 were fixed this session (root causes documented under "Key simulation"/"Perception loop" above); scenario 4 was already passing. Scenario 2 turned out to already be fixed as a side effect of the `uia.js` stdin-vs-file runner fix (its `Cannot read properties of null (reading 'text')` error was the same broken `uia_get_text` call scenarios 3/9 hit) — confirmed with its own individual re-run rather than assumed. **This satisfies the hard gate ("each of these works at all") for the first time**, but not yet the reliability gate: scenarios 5 and 8 are still only skipped (need a real `--interactive` human-typing run and a signed-in JWT respectively — neither done yet), and today's runs are a handful of spot-checks on one machine in one session, not the **10-run/multi-day tally across different sessions/days** the reliability gate actually requires. Next steps: (1) do one real `--interactive` run and one real signed-in run so scenarios 5/8 get an actual pass/fail instead of a skip; (2) start accumulating `--runs=10` results across multiple different sessions/days per the gate below; (3) don't re-enable purchasing/visibility until that tally clears 90%+ per scenario.
- [ ] **(Add new gaps here as they're found.)**

### Gate: hide and disable purchasing until a real readiness bar is met

Until the above is fixed, the app should not be selling something that doesn't work. Concretely:

- [x] **Disable the ability to purchase Pro** (checkout/payment flow) while Phase 0 is unresolved — an admin-only "Purchase Gate" toggle now exists on the Admin page ([Admin.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Admin/Admin.jsx)). Turning it off instantly blocks new/upgraded Pro subscriptions server-side ([postHashData.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/postHashData.js) `subscribeCustomer`, enforced via [purchaseGateController.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/purchaseGateController.js)) — downgrading to Free always still works, so nobody gets stuck.
- [x] **Hide or clearly caveat Simple mentions and entry points** — while the gate is paused, upgrade/"Choose Pro" CTAs are disabled with a "Not available yet" label and a caveat banner on [Pricing.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Pricing/Pricing.jsx), and upgrade buttons/prompts are hidden on [Profile.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Profile/Profile.jsx), [UsageMeter.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/UsageMeter.jsx), and [SimpleChat.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/SimpleChat.jsx). Pricing/plan info itself is left visible (still honest), only the buy action is gated.
- [ ] **Existing Pro subscribers**, if any, should be told plainly what does and doesn't work rather than silently left with a broken paid feature — handle this directly and honestly if it applies.
- [ ] **Core account/transactional email plumbing must be verified working before purchasing is re-enabled** — none of this is about the addon's automation loop, but it's just as much a hard blocker: it's not honest to take someone's money if they can't recover their account or get proof of what they bought.
  - **Purchase confirmation email**: verify `subscriptionCreated` actually arrives, end-to-end, on a real purchase — not just that `sendEmail(userEmail, 'subscriptionCreated', ...)` is called in [postHashData.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/postHashData.js)/[putHashData.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/putHashData.js). Confirm the same for `subscriptionCancelled`/`subscriptionUpdated` in [emailService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/emailService.js), and check delivery (inbox, not spam) with a real provider/account, not just the test-funnel interceptor in [testFunnelController.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/testFunnelController.js).
  - **Forgot/reset password flow**: verify the full loop actually works for a real user — request via [ForgotPassword.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/ForgotPassword/ForgotPassword.jsx) → email arrives (`passwordReset` template, [passwordReset.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/utils/passwordReset.js)) → link lands on [ResetPassword.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/ResetPassword/ResetPassword.jsx) → new password is accepted and actually logs the user in afterward. A subscriber who gets locked out of a paid account with no way back in is a real support/trust problem, not a hypothetical.
  - Note the actual result here (pass/fail, what broke) the same way Phase 0 items above are tracked — don't just check the box on faith.

**Readiness bar for turning purchasing/visibility back on** (a metric worth having, even a rough one, so "ready" isn't just a feeling):
1. **Hard gate — each of these works at all**, tested manually across at least two different common apps: key simulation executes keystrokes/clicks reliably; the perception loop detects and logs user actions without crashing; auto-execution successfully replays at least one captured sequence end-to-end.
2. **Reliability gate — once (1) is true**, run each of the 10 scenarios defined below at least 10 times (in different sessions/days, not 10 back-to-back identical runs, since flakiness from foreground-focus timing or cold-start launches won't show up in a tight loop). Require roughly a **90%+ success rate per scenario** (9/10 or better) before re-enabling purchases and public-facing mentions. If a scenario consistently fails, fix the underlying cause and re-test from zero — don't average it away against passing scenarios or lower the bar.
3. **Account plumbing gate**: purchase confirmation email and the forgot/reset password flow (above) are both manually confirmed working end-to-end with a real email account, in addition to (1) and (2) — this gate is independent of the addon's automation reliability and must pass on its own.
4. Re-run this same check after any significant change to the perception/action pipeline — reliability regressions are easy to introduce silently.

### The 10 validation scenarios, in detail

**Automated runner:** these scenarios can be driven end-to-end (no manual clicking required, except scenario 5) by [validate-core-functionality.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/eval/validate-core-functionality.js). It's a standalone script, deliberately **not** part of `npm test`/`npm run eval` — it drives the real desktop (opens apps, types, clicks, moves the mouse), so it's an opt-in tool for periodically checking the readiness bar above, not something to run on every commit. From `simple-addon/`:
```
npm run validate:core:list        # list the 10 scenarios without running anything
npm run validate:core              # run all 10 once (skips scenario 5 unless --interactive; skips scenario 8 without an LLM token)
node server/automation/eval/validate-core-functionality.js --only=1,2,4   # run a subset
node server/automation/eval/validate-core-functionality.js --runs=10      # repeat for the reliability gate (item 2 above)
node server/automation/eval/validate-core-functionality.js --interactive # also run scenario 5 (needs a human typing)
```
Each run prints a pass/fail/skip line per scenario and writes a JSON log to `simple-addon/server/automation/eval/live-results/<timestamp>.json` — use `--runs=10` across a few different sessions/days and tally the JSON logs to compute the 90%+ reliability number from item 2 above, rather than eyeballing a single run.

These exist so "core functionality works" is a checkable fact, not a feeling. Each scenario names the exact tools involved (matching the real addon code — [input.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/tools/input.js), [open-app.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/tools/open-app.js), [text-type.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/tools/text-type.js), [perception.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/perception.js), [recorder/session.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/recorder/session.js), [recorder/compiler.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/recorder/compiler.js), [agent-loop.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/agent-loop.js), [planner.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/planner.js)) rather than a vague description, a precise pass/fail check, and what it's actually validating. Scenarios 1-4 isolate **key/mouse simulation**; 5-6 isolate the **perception loop**; 7-8 isolate **record→replay** and **planner-driven auto-execution**; 9-10 are safety/edge cases that matter for an honest reliability number.

1. **Type and save a note in Notepad** (basic key simulation + app launch)
   - Steps: `open_app { name: "notepad.exe" }` → wait for window/focus confirmation → `text_type` a known multi-line string (e.g. include punctuation, a number, and a capital letter to catch shift-state bugs) → `input_tap { keys: ["ctrl","s"] }` → the Save-As dialog appears → `text_type` a file name → `input_tap { keys: ["enter"] }`.
   - Pass/fail: the file exists at the expected path afterward, and its contents match the typed string **exactly** (byte-for-byte, including casing/punctuation) — read the file back programmatically rather than eyeballing it.
   - Validates: `open_app` window-ready polling, `text_type` fidelity (no dropped/duplicated/mis-cased characters), `input_tap` modifier-key combos, and that focus actually landed on the app and then the dialog.

2. **Do arithmetic in Calculator by clicking, not typing** (mouse click simulation)
   - Steps: `open_app { name: "calc.exe" }` → `click_at` the buttons for `1`, `2`, `+`, `7`, `=` at their on-screen coordinates (or via UIA element bounds from a perception snapshot, not hardcoded pixels, so it survives DPI/window-size differences) → `perception_recent` to read the result display's text/value.
   - Pass/fail: the displayed result is exactly `19`.
   - Validates: `click_at` accuracy (right pixel, right window focus before the click), and that perception can read back a specific UI element's value rather than just "some snapshot succeeded."

3. **Rename a file in File Explorer** (click + keyboard combo, real filesystem side effect)
   - Steps: pre-create a throwaway test file in a known folder → `open_app` File Explorer to that folder → `click_at` the file to select it → `input_tap { keys: ["f2"] }` to enter rename mode → `text_type` a new name → `input_tap { keys: ["enter"] }`.
   - Pass/fail: the old filename no longer exists, the new filename exists with identical file content/size (nothing corrupted), checked via filesystem stat, not visually.
   - Validates: click-to-select before a keyboard shortcut actually works (a common failure mode is the click landing on the wrong list item or not registering as a selection), and F2/Enter as non-letter "named keys."

4. **Alt-Tab between two open windows** (held-modifier key combo, focus verification)
   - Steps: `open_app` Notepad, then `open_app` Calculator (both left open) → `input_hold { keys: ["alt"] }` → `input_tap { keys: ["tab"] }` while alt is held → release alt → `perception_recent` (or a raw foreground-window title check) to confirm which window now has focus.
   - Pass/fail: the foreground window after the sequence is the one actually expected (not whatever last had focus, not neither) — run it twice, once expecting to land back on Notepad and once on Calculator, to rule out a test that "passes" by coincidence.
   - Validates: `input_hold` release timing/reliability (a key stuck down or released too early is a classic input-simulation bug) and that the perception loop's "what has focus now" answer matches actual OS state.

5. **Detect what the user just typed, without the addon having sent it** (perception-only, human-in-the-loop)
   - Steps: a **human**, not the addon, opens Notepad and types a short known sentence → the addon's perception/recording layer (the same `PollingInputSource` used by [RecorderSession](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/recorder/session.js)) is running and logging in the background → stop the capture and inspect the resulting JSONL log.
   - Pass/fail: the logged event stream reconstructs the same sentence the human actually typed, in the same order, with the correct app/window context attached — not just "some events were logged."
   - Validates: this is the one scenario that's actually about *perceiving what a user does* (the plan's core premise) rather than the addon acting — a prerequisite for "auto action execution" to mean anything, since replay is only as good as what was captured.

6. **Read a non-trivial UI state via perception, not OCR** (perception loop on a real app, not toy text)
   - Steps: open a Settings-style app or dialog with a checkbox/toggle in a known state (e.g. Windows' own "Notifications" or a similar toggle-heavy panel) → call `perception_recent { mode: "interactive" }` → compare the reported toggle state (on/off) to the actual state (verified independently, e.g. by having set it deliberately beforehand).
   - Pass/fail: the reported on/off state matches ground truth for every toggle checked, not just "some nodes came back."
   - Validates: perception returns *usable, correct* state (needed before an agent can safely decide "click this because it's currently off"), not just that a UIA call didn't throw.

7. **Record a short task once, then auto-replay it** (the actual "perceive once, repeat forever" pitch)
   - Steps: start a recording session → manually perform a short, well-defined task (e.g. open Notepad, type "invoice-draft", save) → stop recording → run it through [compiler.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/recorder/compiler.js) to produce a replayable skill/macro → close everything, reset state (delete the saved file) → trigger auto-replay of the compiled macro with no human input.
   - Pass/fail: the replay reproduces the identical end state (same file, same contents) with zero manual intervention, and completes within a reasonable time bound rather than hanging.
   - Validates: the full record → compile → auto-execute pipeline end-to-end — this is the single most important scenario since it's literally the product's core loop as sold.

8. **Give a plain-English instruction and let the planner figure out the steps** (true "auto action execution," not a canned macro)
   - Steps: issue a short natural-language instruction to the agent loop (e.g. "open Notepad and type today's date") without hand-authoring the step sequence → let [planner.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/planner.js) / [agent-loop.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/server/automation/agent-loop.js) decide the tool calls.
   - Pass/fail: the final on-screen/file state matches the instruction's intent (e.g. Notepad actually contains a correctly-formatted current date) — grade the *outcome*, not whether the planner's chosen steps looked reasonable.
   - Validates: the planner can turn intent into correct tool calls without a human pre-scripting them, which is a materially higher bar than replaying a fixed recording (scenario 7) and is what separates "automation" from "AI agent."

9. **Drag-and-drop with a moving mouse path** (continuous mouse motion, not just clicks)
   - Steps: `mouse_path`/`mouse_drag` a file icon from one File Explorer location to another (or drag a slider control to a specific value in a settings app) using a multi-point path with timing offsets, not a single instant jump.
   - Pass/fail: the file actually moved to the target folder (or the slider landed on the intended value, read back via perception) — a drag that "clicks and releases in place" without moving counts as a fail, not a partial pass.
   - Validates: `mouse_drag`'s point-by-point timing actually produces a real OS-level drag gesture, which is a different code path from a single `click_at` and fails differently (e.g. drag threshold not met, drop target missed).

10. **Verify held input releases safely when focus is lost mid-action** (safety/edge case)
    - Steps: begin `input_hold` on a movement key (e.g. holding `w` in a simple full-screen app or game) with `releaseOnFocusLoss: true` → while still "held," programmatically switch focus away (e.g. trigger Alt-Tab to another window) mid-hold → check whether the key was actually released at the OS level (e.g. via a follow-up harmless keystroke test, or process/window state) rather than left stuck down.
    - Pass/fail: the input is released promptly once focus changes — a stuck key (one that keeps "holding" against the wrong window) is a fail even if the initial hold worked, since this is exactly the kind of bug that looks fine in a quick manual test but causes real damage in the field (e.g. a stuck key spamming into whatever the user switches to next).
    - Validates: the safety behavior explicitly documented in `input.js`'s `releaseOnFocusLoss` option actually works, not just the happy path — this is the kind of scenario that's easy to skip when validating "does it work at all" but is exactly what turns into a bad word-of-mouth story if broken.

**How to log results:** for each scenario, keep a simple running tally (date, pass/fail, and — on failure — which step failed and the observed vs. expected state) rather than a single "works/doesn't work" note. That tally is what the 90%+ figure in the reliability gate above should be computed from, and it's also the raw material for Phase 4's "documented, honest reliability rate."

## Phase 1 — Describe what it does, plainly (messaging/copy)

The goal here is not to "sell harder" — it's that a visitor currently can't tell what the product does at all. Being humble doesn't require being unclear. State the facts plainly and let people decide for themselves.

- [x] **Add a plain, factual description of Simple somewhere on the homepage** in [Home.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Home/Home.jsx). Done — added an understated "Simple" section between the links and thank-you tiles (with matching `.simple-section`/`.home-simple-heading`/`.home-simple-desc` styles in [Home.css](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Home/Home.css)) stating plainly what Simple does, with no hype adjectives. The personal-portfolio hero framing ("Steven Tanner Hopwood") is untouched.
- [x] **Rewrite the Pricing page taglines** in [Pricing.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Pricing/Pricing.jsx) for clarity, not persuasion. Done — the vague Free tagline "Get started with the basics" was replaced with "AI chat, full local automation, and 100 MB storage" (a plain statement of what Free includes) in three places: the [Pricing.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Pricing/Pricing.jsx) static fallback, [frontend constants](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/constants/pricing.js), and [backend constants](/c:/Users/tanne/Documents/Github/portfolio-app/backend/constants/pricing.js). The Pro tagline was already plain and is unchanged.
- [x] **Add one concrete, factual example of what it does** near the top of the Pricing/Simple pages — done. Added a flat, non-promotional example line under the Pricing hero ("Simple is an AI agent for your Windows PC: show it once how you rename and file invoices, and afterward saying 'do the invoices' repeats those steps."), and the same example appears in the new homepage paragraph.
- [x] **Leave the About page as-is.** Confirmed — [About.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Simple/About/About.jsx) was not touched in this pass; it stays as the personal bio.

## Phase 2 — Streamline download, install, and update

Right now getting the addon running takes several manual steps: find the GitHub Releases page, pick the right asset, install it, trust a self-signed certificate, and separately notice and act on update banners ([useAddonDetection.js](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/hooks/simpleAddon/useAddonDetection.js) checks GitHub tags and compares semver). For a word-of-mouth product, this first-run friction matters more than any copy: a friend telling a friend "just try it" only works if trying it is actually easy. None of this needs marketing — it needs to be simple.

- [x] **Give the download link a clear, direct destination.** The Download/Update/Get buttons in [Settings.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Settings/Settings.jsx) and [Sidebar.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/Sidebar.jsx) now point at a fixed `ADDON_DOWNLOAD_URL` constant (`.../releases/latest/download/Simple-Addon-portable.exe`, defined in `useAddonDetection.js`) instead of the generic `/releases` list. Made this actually work going forward by giving the portable build a **fixed, version-free** `artifactName` in [simple-addon/package.json](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/package.json) (`Simple-Addon-portable.exe`, was `Simple-Addon-${version}-portable.exe`) — GitHub's `/latest/download/<name>` redirect only works when the asset name is identical across releases. Note: this only takes effect starting with the *next* published release (older releases keep their old versioned filenames), so verify the link 404s or works correctly after the next `addon-v*` tag is published.
- [x] **Add an in-app auto-update check inside the addon itself** — already implemented, not just planned: [auto-updater.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/auto-updater.js) wraps `electron-updater`'s `autoUpdater` (auto-download + install-on-quit, periodic checks every 4h) and is wired up in `main.js` (`new UpdateManager(); updateManager.startPeriodicChecks();`). The addon checks GitHub releases and updates itself in the background without the user needing to revisit the website.
- [x] **Keep the install/update banner copy plain and factual.** Re-audited this against what's actually live after discovering `AddonInstallPrompt.jsx` was dead code (see below) — the plain-text rewrite this item originally claimed ("This adds local AI models, voice control, and system automation...") was made to that dead file and **never actually reached real users**. The real, live banner is `Sidebar.jsx`'s "Enable local addon?" prompt, which turned out to have its own separate bug: it still said **"Connects this page to C‑Simple on your computer"** — the pre-rebrand product name, missed by the `CSimple → Simple` rename commit. Fixed the live text to say "Simple" instead.
- [x] **Reduce the cert-trust step, or explain it in one plain sentence at the moment it's needed.** `Sidebar.jsx` (the `/net` chat sidebar) already showed this explanation, but [Settings.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Settings/Settings.jsx)'s "Get Simple Addon" download link — a separate entry point — did not; a user landing there who already installed the addon and hit the cert wall had nowhere on that page telling them what to click. Wired `useAddonDetection()`'s `addonNeedsCertTrust` flag into Settings.jsx and added the same plain-language note used in Sidebar.jsx ("Browsers block the addon's self-signed cert on HTTPS sites. Click here, choose Advanced → Proceed, then reload this page.") right next to the download link. **Also found and fixed a doc/reality mismatch while auditing this:** `AddonInstallPrompt.jsx` — previously cited in this section as containing the live "Download/Update/Get" buttons — was actually dead code with zero imports anywhere in the app; `Sidebar.jsx` has its own separate, inline implementation of the same install/update/cert-trust UI instead. Deleted `AddonInstallPrompt.jsx` and its CSS as unreachable duplicate code, and corrected the references above that pointed to it.
- [ ] **Consider a single installer instead of "download, then trust a cert, then configure."** If there are multiple manual setup steps today, look at consolidating them into one installer flow (standard Windows installer/MSI, or a setup wizard) so "download and run one file" is the entire experience.
- [x] **Make uninstalling/opting out just as easy as installing.** A product that's easy to remove is more trustworthy to install in the first place.
  - **Opting out of the browser-side integration — done.** `setAddonOptIn(false)` in [simpleAddonApi.js](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/services/simpleAddonApi.js) already worked correctly (clears the opt-in localStorage flag and re-detects immediately), but nothing in the UI ever called it with `false` — the only reachable path was `enableAddonOptIn()` (opt **in** only), leaving a user who turned on browser integration from the "Enable local addon?" banner with no way back except manually clearing `localStorage` themselves. Added a real toggle to [Settings.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Settings/Settings.jsx) (AI & Simple addon section, HTTPS pages only, where the opt-in concept applies) that shows the current state in plain language and lets the user turn it off (or back on) at any time.
  - **Standard Windows uninstall entry — decided: keep portable, document removal.** The site's `ADDON_DOWNLOAD_URL` points at the **portable** exe target (`Simple-Addon-portable.exe` in `simple-addon/package.json`), which never registers in Windows "Add or remove programs" — a user "uninstalls" it by just deleting the file, and any `Start with Windows` login-item registry entry (see `main.js`'s `app.setLoginItemSettings`) is left dangling unless they first flip that off in the tray. Decided to keep the portable exe as the primary download (no install step, the lower-friction path for a word-of-mouth product) rather than switching to the NSIS installer, and to close the silent gap by documenting removal. Added plain, reachable removal instructions to [Settings.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Settings/Settings.jsx) (AI & Simple addon section, next to the download link): right-click the Simple tray icon, turn off **Start at Login** if it's on, choose **Quit Simple Addon**, then delete the downloaded `Simple-Addon-portable.exe`. (NSIS remains available in electron-builder config if a real Add/Remove Programs entry is wanted later.)

## Phase 3 — Make the payment page say what's actually being bought

The current checkout (`Pricing.jsx` → `Pay.jsx` → `CheckoutForm.jsx` → `MembershipPlans.jsx`) presents plans as a features-list comparison. The goal of changes here is accuracy and transparency, not persuasion — a customer should be able to tell exactly what they get and why the price is what it is.

### Guiding principle for what's allowed to differ between tiers

**Only gate functionality that costs real, ongoing money (or your finite personal time) to provide.** Anything that runs entirely on the user's own PC with no server involvement should be free and unrestricted — restricting it anyway is artificial scarcity, which is both less ethical and a worse word-of-mouth story ("it's free but crippled" vs. "the core thing is basically free, we only charge for what actually costs us money").

### What each tier provides today, and what should change

Checked against [backend/constants/pricing.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/constants/pricing.js): `STORAGE_LIMITS` is a real, enforced byte value tied to actual S3 cost. `QUOTAS` (the 50/5,000 commands-per-day figures) is **display text only** — no backend code was found that actually meters or enforces it. So today's comparison is:

> **2026-08-20 update — the "AI chat" row below is now stale and needs re-deciding, not just re-worded.** GitHub Models (models.github.ai) was permanently retired by GitHub on 2026-07-30, which killed the GitHub-PAT BYOK path this row assumed. On 2026-08-13 the backend and addon were migrated to always call **AWS Bedrock (Claude Haiku 4.5)** using the app operator's own AWS credentials — see [llmService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/llmService.js) and [bedrockService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/bedrockService.js). This is now **genuinely server-paid, metered usage** — [apiUsageTracker.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/utils/apiUsageTracker.js) tracks real per-call Bedrock cost against a monthly credit balance via `canMakeApiCall`/`trackApiUsage`, which did not exist when this table was first written. A separate admin-only "Special" flag ([adminController.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/adminController.js)) can grant a specific user unlimited credits without changing their Free/Pro rank — useful for support/testing, but it's an ad-hoc escape hatch, not a documented tier. **None of this is reflected in the table, Pricing page, or Terms yet** — see the new checklist items below.

| | Free | Pro ($15/mo) | Costs real money? | Verdict |
|---|---|---|---|---|
| AI chat (Cloud/AWS Bedrock) | Same fixed model (Claude Haiku 4.5), metered against a monthly credit allowance ($0.50/mo) | Same model, metered against a larger allowance ($10/mo) | **Yes** — every cloud chat call is billed to the operator's AWS account | **Decided.** Metered, server-paid, **no BYOK**. Free and Pro get the same model; they differ only in allowance size, which is now stated plainly in Pricing/Terms. |
| AI chat (Local/HuggingFace) | Included | Included | No — runs on the user's own PC via the addon | Correctly free on both, no change needed. |
| Simple addon, local execution | "Download & run locally" | "Full addon access" | No — runs entirely on the user's PC | **Should not be gated at all.** The Free/Pro distinction here isn't justified by any cost difference — remove it. |
| Automation commands/day | 50/day (fair use) | 5,000/day (fair use) | No — currently unenforced, and even if enforced, a locally-run command costs nothing whether it's the 1st or 5,000th | **Drop this cap as a paid gate.** Automation should be unlimited on Free too, unless a specific step genuinely round-trips through your servers (see caveat below). |
| Cloud storage | 100 MB | 50 GB | **Yes** — real, linear S3 cost | Keep gating this — it's the model done right. |
| Live screen viewing from phone | Not included | Included | **Yes** — needs a cloud relay (server + bandwidth) to get video from PC to phone | Keep gating this — real ongoing infra cost. |
| Support | Community/self-serve | Email support | Costs your time, a real finite resource | Reasonable to keep as a Pro perk. |

**Caveat to check before finalizing:** if any part of the automation loop genuinely calls your own servers (not just the user's local addon), a quota tied to *that specific server-touching step* would still be legitimate under the "costs real money" principle — but it should be scoped narrowly to that step, not applied as a blanket cap on all local commands.

**Good news: a version of this already exists elsewhere in the docs.** [SIMPLE_MARKETPLACE_PLAN.md](/c:/Users/tanne/Documents/Github/portfolio-app/docs/implementation/SIMPLE_MARKETPLACE_PLAN.md) §4 ("Monetization gates") already proposes exactly this pattern for planned marketplace features: record/replay, hotkeys, and browsing/downloading skills stay free for everyone (they're local/no server cost), while LLM re-derivation, parameter inference, and vision-repair fallback are Pro-gated specifically because those steps call the backend's LLM. That's the caveat above, already applied correctly — use it as the template when correcting the current command-quota framing. (One item in that same table, "publish skills to marketplace: 1/day free vs. unlimited Pro," is worth revisiting under this same principle when that work is built — publishing costs a small, roughly fixed amount of storage/moderation, so a shared anti-abuse rate limit may fit better than a paid gate.)

- [x] **Remove the "Full addon access" vs. "download & run locally" distinction** in [pricing.js](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/constants/pricing.js) (frontend) and [pricing.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/constants/pricing.js) (backend) — Free now states plainly it includes "full local automation, no daily cap"; the Pro tier's separate "Full Simple addon access" bullet was removed since there's no longer a partial/full split.
- [x] **Remove or replace the daily automation command cap** as a paid-tier gate. Confirmed (via `getMembershipPricing.js`) that `QUOTAS` was display-text only with no backend enforcement anywhere. Kept `QUOTAS`/`QUOTA_SHORT` (frontend/backend) as identical, unified values — `'Unlimited automation commands (fair use)'` — on both tiers instead of the old 50/5,000-per-day split, updated the Pricing/Terms/Profile copy and the AI system-prompt membership blurb in [llmService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/llmService.js) that used to state the old per-day figures, and reworded [Terms.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Terms/Terms.jsx) §6.2 to describe one fair-use policy instead of a tier-based limit table.
- [x] **Rewrite the Pro tier description to reflect only what genuinely differs**: `DESCRIPTIONS[PRO]` now reads "More storage, live phone viewing, and email support" (frontend + backend + Pricing.jsx fallback), matching the corrected feature lists.
- [x] **Verify `BillingDisclosure.jsx` and `LinkBenefits.jsx` are accurate and not generic Stripe boilerplate.** `LinkBenefits.jsx`'s Stripe Link marketing copy checked out — confirmed `'link'` is genuinely enabled in `getPaymentElementOptions` (`stripeHelpers.js`), so it isn't describing an unavailable feature. **`BillingDisclosure.jsx` and `Terms.jsx` §5.4/§5.5 did not match reality**: both promised "you keep paid features until the end of your current billing period" on cancel/downgrade, but `cancelActiveSubscriptions` in [stripeService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/stripeService.js) calls Stripe's immediate `subscriptions.cancel()` (not `cancel_at_period_end`), and `postHashData.js`'s `subscribeCustomer` flips the user's rank to Free synchronously right after — cancellation is actually immediate, not "at period end." Fixed the copy in both files to describe the real (immediate) behavior instead of promising a grace period the code doesn't provide. **Deliberately did not change the cancellation code itself** in this pass: switching to `cancel_at_period_end` would be the more generous, arguably-better UX, but `subscribeCustomer` unconditionally creates a brand-new Stripe subscription on any upgrade — if cancellation became deferred, a user who downgrades then re-upgrades within the same billing period would end up with two overlapping subscriptions (double billing) unless that reactivation path is also handled. Worth doing properly as its own follow-up, not bundled into a docs-accuracy fix.
- [x] **(Bonus fix, discovered during the above audit)** Found and removed a "Test Customer Creation" debug button unconditionally rendered to every real visitor on [CheckoutForm.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Simple/Pay/CheckoutForm.jsx)'s Stripe-error and payment-loading screens (no env/role gate at all) — looked broken/untrustworthy on a real payment page, so it was removed along with its now-unused `createCustomer`/`useDispatch` wiring.
- [x] **Show the user's real usage before they consider upgrading.** [Pricing.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Pricing/Pricing.jsx) now dispatches `getUserStorage()` for logged-in visitors and shows a plain, neutral usage line ("Your current storage use: X of Y on your plan") above the plan cards, reusing the same `/storage` endpoint and Redux state Profile.jsx already relies on. Purely informational — no upgrade framing. **Bonus fix found while wiring this up:** [App.js](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/App.js) never actually registered a route for `/pricing` — `Pricing.jsx` existed but was unreachable dead code, and the "View all plans →" link on [Net.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Simple/Net/Net.jsx) 404'd. Added the missing lazy import + `<Route path='/pricing'>`. Also cleaned up [UsageMeter.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/UsageMeter.jsx), which still had a hardcoded `50`/`5000`-per-day fallback resurrecting the daily-command cap that was already removed backend-side (Phase 3 above) — confirmed via a repo-wide search that no backend code ever sends `dailyCommandLimit`/`dailyCommandsUsed`, so the fallback was dead-but-misleading code, not a live bug; removed the stale default.
- [x] **Make `MembershipPlans.jsx` feature lists plainly describe what each remaining difference means.** The lists are config-driven (served from `backend/constants/pricing.js` `FEATURES` via `getMembershipPricing.js`), and the config now states only cost-justified differences — metered AI credits (not BYOK), full local automation on both tiers, storage, phone relay, support — so the rendered lists are plain and accurate. The Pro card's "Best Value" badge in [MembershipPlans.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/Checkout/MembershipPlans.jsx) was left as-is (a factual statement, not fabricated urgency).
- [x] **Defer subscription cancellation to period end** (2026-09-04). Implemented in [stripeService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/stripeService.js) + [postHashData.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/postHashData.js): downgrading to Free now schedules cancellation via `cancel_at_period_end` (`deferActiveSubscriptionsToPeriodEnd`) instead of immediate `subscriptions.cancel()`, and `subscribeCustomer` no longer flips the user's rank to Free synchronously — the `customer.subscription.deleted` webhook does it at period end, so a user keeps the paid features they already paid for. The reactivation path is also handled: `subscribeCustomer` detects an active subscription with `cancel_at_period_end` set and calls `reactivateSubscription` (un-cancel) instead of creating a duplicate overlapping subscription, closing the double-billing gap called out above. [BillingDisclosure.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/Checkout/BillingDisclosure.jsx) and [Terms.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Terms/Terms.jsx) §5.4/§5.5 updated to describe the new deferred behavior (Terms §10.2 already described it correctly). Backend suite still green, now with dedicated unit coverage — 21 suites / 266 tests, including a new [stripeService.test.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/__tests__/unit/stripeService.test.js) that exercises `listActiveSubscriptions`/`deferActiveSubscriptionsToPeriodEnd`/`reactivateSubscription` and monthly/annual price resolution against a mocked Stripe client. Still pending: a real end-to-end purchase/downgrade/re-upgrade test against Stripe (needs live keys), and confirming the webhook downgrade actually lands at period end.
- [x] **Add an annual billing option** (2026-09-04). Backend: `getOrCreatePriceId` now accepts a `billingInterval` ('month'|'year'), with `STRIPE_PRO_ANNUAL_PRICE_ID`/`TEST_STRIPE_PRO_ANNUAL_PRICE_ID` env overrides and test-mode $144/yr price creation; `subscribeCustomer` accepts and forwards `billingInterval`; `validateSubscription` validates it; `getMembershipPricing.js` returns an `intervals` array (monthly + annual) on the Pro plan when both prices exist. Frontend: `MembershipPlans.jsx` shows a plain Monthly/Yearly toggle (via `useCheckoutState.billingInterval`), and the confirmation step/`getPlanDisplayName` show the chosen cadence. The annual option only appears when an annual Stripe price actually exists for the Pro product (live mode lists prices by interval) — so it needs a real $144/yr price created in the Stripe dashboard before it's visible in production.
- [x] **~~Confirm the free tier's "bring your own API key" option~~** — moot. BYOK is removed entirely (decision below); there is no bring-your-own-key option to explain. The Free tier's AI chat is metered against an included monthly cloud-credit allowance.

### OCR: platform-funded and metered (decided — no BYOK option)

Tracing the code turned up a real gap worth acting on: [backend/services/ocrService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/ocrService.js) uses the platform's own `OPENAI_KEY` (`process.env.OPENAI_KEY`) for OCR image-to-text extraction *and* its optional LLM post-processing step.

- [x] **Decide OCR's model deliberately** — decided: OCR stays **platform-funded** (no BYOK, consistent with the global no-BYOK decision below). Its LLM post-processing step already routes through `canMakeApiCall(userId, 'openai')` in `postProcessWithLLM` and is therefore metered against the same per-tier monthly credit allowance as AI chat.
- [x] **Meter OCR image-to-text extraction** — done, alongside repairing the OCR flow itself (it was broken end-to-end, not just unmetered): the frontend ([useOcrExtraction.js](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/hooks/useOcrExtraction.js)) sends `imageUrl`/`s3Key`/`method`, but [ocrController.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/ocrController.js) expected `imageData`/`ocrProvider`, and the service had positional signatures the controller didn't match — so every `/ocr-extract` request 400/500'd (see [debug-ocr.md](/c:/Users/tanne/Documents/Github/portfolio-app/docs/debugging/debug-ocr.md)). Fixed the controller to match the frontend contract (fetch the image from S3 via a new `getFileBuffer` in [s3Service.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/s3Service.js), or from `imageUrl`, or legacy base64 `imageData`), gate paid providers via `canMakeApiCall`, and deduct an estimated per-call cost via `trackApiUsage`. `tesseract` stays local and free. **Also fixed the backend Jest suite**: it previously couldn't parse the AWS SDK's ESM browser build (`Unexpected token 'export'`) because `testEnvironment` was `jsdom`, which made Jest resolve the SDK's browser/ESM entry — switched the backend Jest `testEnvironment` to `node` (the backend tests don't use DOM), and the full suite now passes (14 suites / 182 tests). [test-ocr.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/__tests__/integration/test-ocr.js) now has 10 contract tests covering image resolution, the credit gate, and response shapes.
- [x] **Mention OCR in the tier picture** — OCR is a platform-paid feature included on both tiers and metered against the same AI credit allowance; no separate tier gate. (Not added as a feature-list bullet since it isn't a Free/Pro differentiator.)

### BYOK is permanently off the table (decided 2026-08-27)

**Decision: there is no bring-your-own-key (BYOK) path, now or planned.** All cloud AI runs on the operator's own accounts — AWS Bedrock (Claude Haiku 4.5) for chat, the platform's own OpenAI key for OCR post-processing — and is metered against a per-tier monthly credit allowance. No user-supplied API keys are accepted anywhere. (Historical note: a GitHub-PAT BYOK path once existed; GitHub retired GitHub Models on 2026-07-30, which broke it, and the migration to metered Bedrock replaced it on 2026-08-13. This closes the question rather than merely deferring it.)

- [x] **Do not revive BYOK** — decided: no. The per-user-key extension point (an empty `PER_USER_KEY_PROVIDERS` set in `apiUsageTracker.js`, the `perUserKey` flag check in `llmProviders.js`, and the "future BYOK provider" comments) was removed rather than left as a dormant hook.
- [x] **Update all AI-describing copy to state the real model** — done: pricing config (frontend + backend), the Pricing page fallback, the `llmService.js` membership system-prompt blurb, and Terms §3/§4 now all state that AI is metered, credit-based, and server-paid, with no BYOK option.
- [x] **Document the admin "Special" unlimited-credits flag** (`isSpecialUser`, toggled from the Admin user-management table) — done: see [special-user-flag.md](/c:/Users/tanne/Documents/Github/portfolio-app/docs/implementation/special-user-flag.md). (Previously a code-only exception; the `41a31e6` credit-tracking bug — a stale-cache write that silently wiped a user's `Special` flag and briefly overwrote their password hash with `'[redacted]'` — was exactly the kind of quiet exception this doc now guards against.)
- [x] **Decide whether the free-tier AI credit ceiling needs its own visible usage meter** — decided: it already has one, so no new meter is needed. [UsageMeter.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/UsageMeter.jsx) (rendered at the bottom of the chat sidebar in [Sidebar.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/Sidebar.jsx)) shows `$used / $limit` against the per-tier monthly credit allowance from `/api/data/usage` ([getUserUsageStats](/c:/Users/tanne/Documents/Github/portfolio-app/backend/utils/apiUsageTracker.js)), with a percent bar and a "last reset" date, and it auto-expands for Free users so the ceiling is visible ahead of time rather than discovered as a surprise block.

## Phase 4 — Keep proving reliability with real users (after Phase 0's bar is met)

Per the business plan, reliability of the core perceive→act loop is the actual product risk, not pricing structure. Phase 0 covers getting the core to a working, gate-passing state before it's sold at all; this phase is what continues once that's true and purchasing/visibility is back on.

- [ ] **Keep the same narrow, well-defined scenarios from Phase 0's readiness bar as an ongoing regression check** — re-run them periodically, not just once, so a later change doesn't silently break what you validated.
- [ ] **Instrument success/failure of automation runs** in real usage (if not already tracked) so you know the real-world reliability rate, not just the rate from your own test scenarios — this number should directly inform what you're comfortable stating on the pricing/product pages. Don't describe more than the current reliability rate supports.
- [ ] **Listen to the first real Pro subscribers.** A handful of direct, low-key conversations ("what made this useful for you? what's missing?") should feed back into Phase 1/2/3 copy — this is about listening, not soliciting testimonials for marketing.

## Phase 5 — Let the free→paid path be honest and low-pressure

- [x] Add an in-app usage meter for **storage** (the one real, cost-based limit) — done. Storage usage was already visible in [Profile.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Profile/Profile.jsx) (full meter with a progress bar and near/over-limit warnings) and as a one-line notice in [Pricing.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/pages/Pricing/Pricing.jsx). The missing spot was the main chat experience, so a compact [StorageMeter.jsx](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/SimpleAddon/StorageMeter.jsx) now renders at the bottom of the chat sidebar (below the credits meter) showing "X / Y used" with a thin bar, plus a plain "You're using N% of your storage" notice when near (≥80%) and "You've used all of your storage — you can free up space or upgrade for more" when over limit. No meter is needed for automation commands (that cap is removed per Phase 3).
- [x] Replace any hard block at the storage limit with a plain, factual notice — partly done, plus a real bug found and fixed. The frontend was silently swallowing save failures: [dataService.createData](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/features/data/dataService.js) called `handleTokenExpiration(error)` and returned `undefined` without re-throwing, so the thunk resolved as success and a failed save never reached the user. Fixed: `createData` now re-throws, and the `createData` thunk surfaces the backend's `details`/`error` (e.g. a 413 "Storage limit exceeded") so `.unwrap()` callers (comments, support, game saves) show a factual error instead of pretending success. Profile already showed over-limit text from real usage. **Follow-up found while tracing this**: the tier storage check (`trackStorageUsage` → 100 MB / 50 GB) lives in [postData.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/postData.js), which is mounted on `POST /api/data/public` (no `protect`), so `req.user` is absent there and that check appears not to fire for the authenticated save path ([postHashData](/c:/Users/tanne/Documents/Github/portfolio-app/backend/controllers/postHashData.js), which has no storage check) — the limits actually enforced on saves are the per-item 350 KB files / 400 KB item caps in [dataService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/dataService.js). **Done (2026-09-04):** the tier storage limit is now enforced on the authenticated save path — [dataService.js](/c:/Users/tanne/Documents/Github/portfolio-app/backend/services/dataService.js) `createDynamoDBItem` calls `trackStorageUsage(userId, item)` before writing and returns a 413 with `details` (current usage / item size / limit) when over the 100 MB Free / 50 GB Pro cap; an error in the storage *check itself* is logged and does not block the save (matching `postData.js`'s behavior), so an unrelated DynamoDB/Stripe outage can't lock users out of saving.
- [ ] If you want to sanity-check the $15/mo Pro price is reasonable, it's fine to quietly compare a couple of price points across separate signup cohorts — but treat it as a fairness/affordability check, not a persuasion experiment, and don't let it drive aggressive framing.
- [ ] Only after the above: revisit Tier 2/3 ideas from [ETHICAL_MONETIZATION_STRATEGIES.md](./ETHICAL_MONETIZATION_STRATEGIES.md) (one-time top-ups, lifetime unlocks, supporter tier) — each still needs its own validation step (survey/support-request signal) before building, and each should stay low-key and optional rather than promoted hard. Note: a "storage top-up pack" remains a legitimate cost-based idea; a "command top-up pack" no longer fits once commands aren't gated.

---

## Phase 6 — Lightweight in-addon dashboard UI (supplement, not replacement, for the webapp)

Right now, many addon capabilities only exist behind the system tray's right-click menu ([tray.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/tray.js)) — a native context menu that has grown to include eye tracking, the automation agent, wakeword, goal-from-clipboard, the recorder/skills flow, workspace profiles, the permission center, dry-run/kill switch, updates, and more. Several of these already spawn scattered, single-purpose `BrowserWindow` popups ([permissions.html](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/permissions.html), [recordings.html](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/recordings.html), [workspace-prompt.html](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/workspace-prompt.html), [settings.html](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/settings.html)) with no shared navigation or visual consistency, and native-menu prompts (`prompt()`/`confirm()`) for things like naming a recording. This phase consolidates all of that into a single, lightweight, on-demand dashboard window in the addon itself.

**This is explicitly a supplement to the webapp UI, not a replacement for it.** Account, billing, chat, and any other feature that already lives in [frontend/](/c:/Users/tanne/Documents/Github/portfolio-app/frontend) stays there — the dashboard only houses local-machine, addon-only controls that don't need a browser/account context (agent, recorder, workspace layout, permissions, eye tracking, update status). If a control genuinely needs the user's account/cloud state, it belongs in the webapp, not here.

### Guiding principles
- **Supplement, don't duplicate.** Nothing that already exists in the webapp should be rebuilt in the dashboard. Scope is strictly local/addon-only controls currently buried in the tray menu or scattered across separate popups.
- **Keep it lightweight — no new framework/bundler.** Continue the existing pattern already used by `permissions.html`/`recordings.html`: plain HTML/CSS/JS renderer pages loaded via `file://` with Electron `contextIsolation`, talking to the local Express server via `fetch`. This avoids adding a build step or a new dependency (React/Vite/etc.), keeps window-open time instant, and matches the existing renderer code style so it stays maintainable without relearning a framework later.
- **One window with tabs/sections, not many popups.** A single `BrowserWindow` ("Simple Dashboard") replaces the current one-popup-per-feature sprawl, opened from the tray's new "Open Dashboard" item (and probably the tray icon's double-click, replacing or supplementing "Open Web App" — decide during implementation which single-click behavior makes more sense).
- **The tray menu shrinks to essentials only.** After this ships, [tray.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/tray.js)'s right-click menu should contain roughly: Open Dashboard, Open Web App, Start at Login, Quit, and a couple of true one-click safety actions that must survive even if the dashboard window fails to open (e.g. Emergency Stop / Kill Switch). Everything else (agent controls, eye tracking, wakeword, recorder/skills, workspace profiles, permission center, dry-run toggle, resources folder, update status/actions) moves into the dashboard.

### Planned dashboard sections (tabs), consolidating tray items + existing popups
- [x] **Status** (replaces `settings.html`): server/HTTPS port, Python status, addon version, restart-server button, resources folder path + change button. **Done**: implemented as the dashboard's first tab in [dashboard.html](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/dashboard.html), backed by a small [dashboard-preload.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/renderer/dashboard-preload.js) IPC bridge (`dashboard:get-status`/`restart-server`/`setup-python`/`open-resources-folder`/`change-resources-folder`/`get-start-at-login`/`set-start-at-login` handlers in `main.js`), since this tab's data — server port, Python status, resources path, login-item setting — lives only in main-process memory, not behind an HTTP endpoint. A `dashboard:status-changed` push event notifies the open dashboard immediately on server/Python state changes, backed by a 10s poll as a fallback. `settings.html`/`preload.js` were confirmed dead code (no `ipcMain` handlers for `get-status`/`restart-server`/`get-version` existed, and `settingsWindow` was declared but never opened) — left in place for now, to be deleted alongside the other legacy renderer pages once all tabs are done (see Implementation approach below).
- [x] **Agent**: running/idle state, current goal, start/stop, dry-run toggle, kill switch, "Create Goal from Clipboard", wakeword start/stop — currently tray-only, no window today. **Done**: implemented as the dashboard's second tab, talking directly to the existing local HTTP endpoints (`/api/agent/start|stop|status`, `/api/automation/permissions` for dry-run, `/api/automation/permissions/kill`, `/api/voice/wakeword/start|stop`, `/api/agent/goal-from-clipboard`) via the same `fetch`-based `api()` helper pattern already used in `recordings.html` — no IPC or backend changes needed since these were already server HTTP routes, just tray-menu-only in the UI. Polls `/api/agent/status` every 2s while the agent is running (mirroring `startAgentStatusPolling()` in `main.js`) and shows a live "running" badge next to the Agent tab in the sidebar. **Left in tray for now** (per the incremental migration approach) until all tabs are done. **Workers + live activity added (2026-09-04):** the Agent tab now shows the multi-agent pool's goal-bound workers (from `workers[]` in `/api/agent/status`, each with a per-worker **Stop** via `DELETE /api/agent/worker/:goalSlug`), and a **"Live activity"** feed that streams `/api/agent/events` (filtered to `agent.step`/`agent.message`/`agent.stopped`/`skill.run`/`goal.created`/`approval.pending`) into a newest-first list — the same "watch the agent work" signal the web app's live panel provides, now native to the dashboard. (The multi-agent pool and the SSE route were already built + tested but had no dashboard surface.) **Goals panel added (2026-09-04):** a 🎯 Goals panel lists the workspace goals via `GET /api/agent/goals` with per-status badges, and a **▶ Run** per *active* goal calls `/api/agent/start {goalSlug}` (running it as a goal-bound worker via the multi-agent pool) — so a signed-in customer can pick *which* goal to work on, not just "next active goal". Full goal CRUD stays in the web app's GoalManager per the supplement-don't-duplicate rule.
- [x] **Agent → Automation suggestions (pattern learner, fixed + surfaced 2026-09-04)**: the proactive-suggestions feature existed but was silently dead — `pattern-learner.js`'s `_fetchEntries()` called `this._wsClient.req(...)`, but `req` is not part of `workspace-client.js`'s public exports, so every analysis fetched zero action-log entries and `/api/agent/suggestions` always returned `[]`. Fixed by adding + exporting `getActionLog({ days, n })` on the workspace client and pointing the learner at it (with a defensive fallback). Then added a `POST /api/agent/suggestions/accept` route that turns an accepted suggestion into an agent goal (`wsClient.upsertGoal`) — deliberately a *goal* rather than a fabricated skill, since the learner keeps tool fingerprints, not exact args, so rebuilding precise steps would be guesswork. Surfaced it as an "Automation suggestions" panel in the Agent tab (Check for patterns → list with title/description/confidence/repeat count → Accept). Added `pattern-learner.test.js` (10 tests: fingerprinting, tokenization, sequence detection, action-log fetch) and registered it in `npm run test:unit`. Note: suggestions require the web-app sign-in (the action log lives in the cloud workspace); the LLM naming path is intentionally left un-wired (falls back to generic titles) since it would need a backend-proxied LLM client — a possible later enhancement.
- [x] **Recorder & Skills** (replaces `recordings.html` as a tab instead of its own window): start/stop recording, add marker, list of saved recordings, compile-to-skill modal, run-a-skill-by-slug. **Done**: ported as the dashboard's third tab, reusing the exact same `/api/recorder/*` and `/api/skill/*` endpoints and UI flow `recordings.html` already used (status polling while recording, recordings list with Compile/Delete, a review-and-name modal before saving a compiled skill, and a run-by-slug affordance since there's no list endpoint for the local skill cache). `recordings.html` + its `BrowserWindow`/tray entry are intentionally **left in place for now** (not yet deleted) until this tab gets a real manual verification pass in a running Electron session — see the "Implementation approach" note below about not removing legacy popups until each migrated tab is confirmed working, not just implemented. **Parameter support added (2026-09-04):** skills that declare `params` (an array of `{name,description,type}`, produced by the parametrize flow or authored by hand) now show a param count in their card, and **▶ Run** opens a "Run with parameters" modal that collects one value per `${param.x}` placeholder and passes them to `/api/skill/run` — previously every run hardcoded `params: {}`, so a parametrized skill would fail with `missing skill param`. Also added a **Parametrize** action on recordings (shown once there are 2+): pick a second recording of the same task and it calls `/api/skill/infer-params` to diff the two demonstrations and promote what varies (names, values, typed text) into `${param.x}` placeholders, opening the result in the review modal to inspect and save. This closes the loop with the Triggers tab's file-watch params: a customer can record a task twice with different inputs to get a reusable skill, then point a folder-watch trigger at it and have `${param.file}` resolve to the exact file that fired it. **Capability review + AI generalization added (2026-09-04):** the skill editor modal (Edit / Review / New skill) gained two more actions. **"What will this do?"** calls `/api/skill/capabilities` and renders a plain-English action list, per-step category badges (safe-read / sandboxed-write / shell / destructive / system), and any declared-vs-actual category mismatch warnings — a free, local trust step so a customer sees exactly what a skill will do before running it (the §6.2 pre-run summary, previously backend-only with no UI consumer). **"Make robust with AI"** calls `/api/skill/generalize` (Bedrock via the backend, same path as the NL compiler — needs a signed-in user, Pro-gated per the marketplace plan §4) to re-derive a literal recording into stable UI-level steps (`uia_invoke`/`click_visual`/`type_text` instead of raw `click_at` coordinates), showing a `generalized`/`fromCache` note on success and gracefully keeping the original steps on failure — the §5.1 "priority-one fix" for the compiler's literal-coalescing weakness, now reachable from the dashboard instead of only via API. **Natural-language editing added (2026-09-04):** a **"Modify with AI"** action joins the other two in the same modal — it prompts for a plain-English change ("press z after the shift click"), calls `/api/skill/edit-natural` (backend Bedrock, same signed-in/Pro path as the other AI actions), and replaces the editor's steps with the modified array (showing old→new step count), so a customer can tweak a recorded macro without hand-editing JSON. This was already built + tested (`nl-compiler.test.js` `editSteps`, 7 cases) but had no dashboard consumer. **Local run history added (2026-09-04):** a new `run-history.js` module + `GET|DELETE /api/skill/runs` endpoints record every skill run (manual, hotkey, or trigger-fired — the `/api/skill/run` handler now appends a `{slug, startedAt, durationMs, stepsRun/stepsTotal, failed, outcome, error, triggerId, source}` entry to `%APPDATA%/simple-addon/run-history.jsonl`, capped at 200). A **"Recent runs"** panel in the Recorder & Skills tab lists them newest-first with pass/fail badges and the failure's error, auto-refreshing after each manual run — so a customer can confirm their 9am trigger actually fired and succeeded without watching it live (the run console only covers a run while it's in flight, and the event ring buffer doesn't survive a restart). `run-history.test.js` (14 tests) registered in `npm run test:unit`. **On-disk skill persistence added (2026-09-05):** the local skill cache was in-memory only, so a skill saved while signed out (when the workspace sync best-effort fails) was silently lost on addon restart — contradicting the save modal's "saved locally" message. `tools/skill.js` now mirrors every `cacheSkill()` write to `%APPDATA%/simple-addon/skills/<slug>.json` (best-effort, never throws) and lazily loads that directory into the in-memory cache on first access (`_ensureDiskLoaded()`), so signed-out skills survive restarts exactly like recordings already did. `uncacheSkill()` deletes the disk file too. `skill.test.js` now 40/40 (added 4 tests: disk round-trip, uncache removal, `getAllCachedSkills` inclusion, and a pre-seeded-file "restart survival" test proving a fresh process reloads persisted skills). This stays fully local/on-device, so it remains free on both tiers per the cost-based tiering rule. **Skill export/import added (2026-09-05):** a new `GET /api/skill/export` (all skills, or `?slug=` for a single one) and `POST /api/skill/import` (accepts a single skill object, an export bundle `{skills:[...]}`, or a plain array) give customers backup, portability, and sharing with no cloud round trip — import reuses `cacheSkill()` so restored skills land in memory + on disk, then best-effort syncs to the workspace when signed in. The Recorder & Skills tab gained **Export all** (downloads a `simple-skills-YYYY-MM-DD.json` backup) and **Import** (file picker → validates → reports imported/skipped/sync-failed counts → refreshes the list). `skill-export-import.test.js` (11 tests) exercises the real `mountAutomation()` app offline via `eval/http-app.js` with APPDATA redirected (export-all/single/missing-404; single-object import; bundle import with a malformed-step skip; missing-slug skip; garbage-body 400; disk persistence). Free on both tiers (no server cost).
- [x] **Workspace Profiles**: save-new (the separate `workspace-prompt.html` name-prompt window becomes an inline in-tab form instead of its own `BrowserWindow`), list/restore/update/delete existing profiles — currently tray-submenu-only for restore/update/delete. **Done**: implemented as the dashboard's fourth tab using the existing `/api/workspace-profiles` HTTP API (`GET` list, `POST` save-new, `POST /:name/restore`, `POST /:name/update`, `DELETE /:name`) — note this tab talks to the HTTP endpoints rather than `require()`-ing `workspace-profiles.js` directly the way `main.js`'s tray callbacks do, since the renderer can't reach Node modules directly under `contextIsolation`. The "Save New…" flow is a plain inline text input + button instead of the separate `workspace-prompt.html` popup + its own preload/IPC round-trip. `workspace-prompt.html`, its preload, and the tray's Workspace submenu are **left in place for now** pending manual verification, same as the Recorder tab above.
- [x] **Permissions** (replaces `permissions.html` as a tab): the existing Permission Center content, re-hosted rather than rebuilt. **Done**: ported as the dashboard's fifth tab — global controls (dry-run, network binding, kill switch), pending approvals, category defaults, per-tool overrides, shell command allow/deny filters, and filesystem sandbox roots, all calling the same `/api/automation/permissions|tools|pending-approvals|approve` endpoints `permissions.html` already used (via a small `automationApi()` helper mirroring that page's `api()` function, kept separate from the dashboard's general `api()` helper since it targets a different base path). `permissions.html` + its tray "Open Permission Center" entry are **left in place for now** pending manual verification, same as the other migrated tabs above.
- [x] **Eye Tracking**: enable/disable, calibrate, overlay test-mode toggle, emergency stop, help text — currently tray-only. **Done**: implemented as the dashboard's sixth tab. Unlike the other tabs, this one talks to the main process over IPC rather than the local HTTP server (`dashboard:get-eye-tracking-status`/`toggle-eye-tracking`/`toggle-eye-overlay`/`emergency-stop-eye-tracking`/`calibrate-eye-tracking` handlers added to `main.js`, exposed via `dashboard-preload.js`), because the real state (`server.eyeTrackingManager`) and the existing calibration/overlay window-management functions (`openCalibrationWindow()`, `startEyeOverlayMode()`/`stopEyeOverlayMode()`) already live in main-process memory — this avoided adding new HTTP routes for state the app already tracks natively. **Calibration itself was deliberately not re-implemented inline**: it's a multi-step interactive flow (camera capture + gaze-point validation) with its own dedicated window and preload (`calibration.html`/`calibration-preload.js`); the dashboard's "Calibrate" button just opens that existing window rather than duplicating its logic, consistent with the "supplement, don't duplicate" principle applied internally to already-complex sub-flows. `server.eyeTrackingManager.onStateChange` now also calls `notifyDashboardStatusChanged()` so an open dashboard reflects state changes (e.g. Escape/Ctrl+Alt+E emergency stop) without waiting on its poll. These tray items are **left in place for now** pending manual verification, same as the other migrated tabs above. **Calibration quality + error diagnostics added (2026-09-05):** the Eye Tracking tab now shows the calibration summary (point count, mean error in px, model type, last-calibrated time) via a new `getEyeCalibrationSummary()` preload bridge (reusing the existing `get-prior-calibration` IPC channel already used by the calibration window — no new main-process handler), a live "tracking for Ns · camera i" line while active, and the last error (e.g. "No calibration data found") when a start fails — so a customer can see *why* eye tracking won't start and whether their calibration is actually accurate, without opening the calibration window. `dashboard:get-eye-tracking-status` now returns the manager's authoritative `state`, `hasCalibration`, `lastError`, `elapsed`, `duration`, and `cameraIndex` (via `eyeTrackingManager.getStatus()`) instead of just the tray's lagging state + overlay flag. **Tracking-camera correctness fix + camera preview added (2026-09-05):** the dashboard's Enable toggle previously called `eyeTrackingManager.start()` with no camera, defaulting to camera 0 — while `startEyeOverlayMode()` correctly read the calibration file's `cameraIndex`. A multi-camera user who calibrated on a non-zero camera got garbage gaze from camera 0. Fixed by extracting `resolveCalibrationCameraIndex()` (reads `eye-calibration.json`'s `cameraIndex`, falls back to 0) and using it in both `dashboard:toggle-eye-tracking` and the overlay path. Also added a **Camera** section to the Eye Tracking tab: a camera dropdown (from `getEyeCameras()` → the existing `get-cameras` IPC) and a **Preview** button (from `getEyeCameraSnapshot()` → `get-camera-snapshot`) showing a live snapshot of the selected webcam — so a customer can confirm the right camera is selected and pointed at their face without opening the calibration window. **Gaze-pipeline wiring fix + live gaze readout added (2026-09-05):** `EyeTrackingManager` was a plain class, but `perception-bus.js` subscribed via `eyeTrackingManager.on('gaze', …)` — which silently never fired, so gaze never reached the perception bus; separately, `main.js`'s `onGazeData` forwarding was captured once at init then overwritten by the overlay/validation windows. Made `EyeTrackingManager` extend `EventEmitter` (emits `'gaze'` per frame in the stdout parser) so the perception bus's existing subscription now works during plain tracking, overlay, and validation alike, and replaced `main.js`'s fragile wrapper with a `'gaze'` listener that forwards live gaze to an open dashboard. The Eye Tracking tab now shows a live `Gaze (x, y) · confidence N` readout while tracking. Added `eye-tracking-manager.test.js` (8 tests: EventEmitter contract, gaze emission, status/summary shape, online-training no-op guards) — registered in `npm run test:unit`.
- [x] **Updates**: current update state (idle/available/downloading/ready/error), check/install actions — mirrors `_buildUpdateMenuItems()`'s tray logic today. **Done**: implemented as the dashboard's seventh and final planned tab, calling the existing `/api/update/status|check|install` HTTP endpoints (`update-bridge.js`) that already power the web frontend's single-click update flow — no new backend code needed. Polls every 2s while a download is in progress (mirroring the tray's own progress display) and shows a live sidebar badge (downloading/ready/error) matching the pattern used for the Agent tab's "running" badge.
- [x] **Triggers** (added 2026-09-04, after the original 7 tabs): schedule/file-watch/hotkey automation. The trigger engine (`automation/triggers.js`) already existed and was wired to `/api/triggers` CRUD + `main.js` dispatch, but had no dashboard UI — customers couldn't configure it without editing JSON. Added a Triggers tab that lists active triggers (enable/disable/delete) and a form to create a cron schedule ("every day at 9am"), a folder-watch ("when a *.pdf lands in Downloads"), or a global hotkey. Each trigger targets **either a goal or a recorded skill (macro)**: goals come from a new `GET /api/agent/goals` route (wraps `workspace-client.listGoals()`, needs a signed-in user), while skills come from the existing `GET /api/skill` list. `triggers.js`/`main.js` now accept `skillSlug` in addition to `goalSlug`, dispatching skill triggers to `/api/skill/run` (fully local, deterministic, no LLM/cloud — so "run my invoice macro every day at 9am" works even signed out) instead of `/api/agent/start`. This is the addon-side first step toward the Tier 2 "scheduled automation reminders" idea from [ETHICAL_MONETIZATION_STRATEGIES.md](./ETHICAL_MONETIZATION_STRATEGIES.md), and it's local/on-device, so it stays free on both tiers per the cost-based tiering rule. **File-watch triggers now pass the fired file as skill params** (2026-09-04): `triggers.js` includes `fullPath` in the trigger's `_firedBy` and adds a `fileTriggerParams()` helper mapping it to `{ file, filename, eventType }`, which `main.js` forwards as the `params` argument to `/api/skill/run` — so a skill can use `${param.file}` / `${param.filename}` / `${param.eventType}` placeholders to act on the exact file that triggered it (e.g. a "process PDFs" skill watching Downloads). The Triggers tab's folder-watch hint documents these placeholders. `triggers.test.js` now 23/23 (added 4 `fileTriggerParams` tests). **Trigger "test now" added (2026-09-05):** a new `POST /api/triggers/:id/test` endpoint fires a trigger's target immediately — skill triggers run the skill through the same permission-gated registry path (publishing `skill.run` + a `source: 'trigger-test'` run-history entry so it shows in Recent runs), and goal triggers start the goal as a pool worker. Each trigger card in the Triggers tab gained a **▶ Test** button; for file-watch triggers it optionally prompts for a path to simulate the fired file, so a customer can verify `${param.file}` placeholders resolve without waiting for the real event.
- [x] **Voice** (added 2026-09-04): surfaces the rest of the voice pipeline that was previously only reachable via the wakeword button in the Agent tab. The `/api/voice/*` endpoints (listen/speak/stop/status, backed by `audio-stream-manager.js` + the Python voice pipeline) already existed but had no dashboard UI. Added a Voice tab with a live status line (listening/wakeword/model/device), a **Start Listening** → transcription box (`POST /api/voice/listen`), a **Speak** text-to-speech box (`POST /api/voice/speak`), and an audio-devices list (from `GET /api/voice/status`'s `devices` field). Wakeword control stays in the Agent tab to avoid duplicating it. Note: voice runs locally on the user's PC, so it remains free on both tiers per the cost-based tiering rule. **Microphone + model selection added (2026-09-05):** `audio-stream-manager.js` already had `setDevice(index)`/`setModel(size)` but there was no HTTP route, and the Voice tab rendered the microphone list read-only — a customer with multiple mics (or wanting a faster/more-accurate Whisper model) couldn't choose. Added `POST /api/voice/device` (validates a non-negative integer index) and `POST /api/voice/model` (validates `tiny|base|small|medium`) wired to the manager, and updated the Voice tab: a **Speech model** dropdown plus a per-device **Use** button with a `✓ In use` marker on the active mic. Added `voice-routes.test.js` (5 tests covering the offline input-validation 400 paths; the happy path needs a real microphone + the Whisper model download).
- [x] **Perception** (added 2026-09-04): surfaces the perception bus so a customer can see what Simple currently perceives — the thing the whole product pitch is built on. The `/api/perception/status|frame|history` endpoints (backed by `perception-bus.js`) existed but had no UI. Added a Perception tab with a live status line (running + which sensors — window/screen/audio/gaze — are active + last-frame age), a **Current view** box showing the human-readable `context` string from `GET /api/perception/frame` (foreground window, last-heard audio, gaze, recent actions), and a **Recent perception** history list. Auto-refreshes every 5s. This doubles as a debugging aid for the Phase 0 perception-loop work — it makes "does perception actually see what the user did" directly inspectable instead of a matter of reading logs.

### Implementation approach
- [x] New renderer page `simple-addon/renderer/dashboard.html` (+ a small preload only if IPC beyond `fetch`-to-local-server is actually needed) implementing a tabbed shell and each section above, reusing the dark theme/CSS variables already established in `permissions.html`/`recordings.html` for visual consistency rather than inventing a new look. **Done**: all 7 planned tabs (Status, Agent, Recorder & Skills, Workspace Profiles, Permissions, Eye Tracking, Updates) are now implemented — see the "Planned dashboard sections" checklist above for per-tab detail. Most talk to the existing local HTTP API via the same `fetch`-based pattern `recordings.html`/`permissions.html` already used; the Status and Eye Tracking tabs use a small `dashboard-preload.js` IPC bridge instead, since their real state/actions (server/Python status, the eye-tracking manager, the calibration window) live only in main-process memory with no existing HTTP route.
- [x] New `openDashboard()` window-management code in [main.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/main.js), following the same single-instance-focus pattern the existing windows already use (`if (window && !window.isDestroyed()) { window.focus(); return; }`). **Done**, and now also accepts an optional initial-tab argument (`openDashboard('permissions')`) passed through as a `?tab=` query param so a caller can jump straight to a specific section.
- [x] Update [tray.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/tray.js)'s `_updateMenu()` and its callbacks to trim the menu down to the essentials list above. **Done**: after manual feedback that navigating via the tray to reach individual popups was the actual pain point, the tray now only shows Open Dashboard, Open Web App, the two safety-critical actions (Kill Switch, Eye Tracking Emergency Stop), Start at Login, and Quit — every other item (agent controls, eye tracking toggles/calibrate/help, recorder/skills, workspace profiles, permission center, dry-run, resources folder, Python setup, restart server, update check/install) was removed from the menu template now that the dashboard covers all of it. The tray icon's single left-click now opens the dashboard directly (`tray.on('click', ...)`); double-click still opens the web app for anyone used to that gesture.
- [x] Once each section is confirmed working inside the dashboard, delete `permissions.html`, `recordings.html`, `workspace-prompt.html`, `settings.html` (+ their preloads) and the corresponding `BrowserWindow` creation code/globals in `main.js` — don't leave duplicate or dead UI paths behind after migrating. **Done**: deleted `permissions.html`, `recordings.html`, `workspace-prompt.html`, `workspace-prompt-preload.js`, `settings.html`, and the top-level dead `preload.js` (confirmed unused — see the Status tab note above), along with their `BrowserWindow` globals (`permissionWindow`, `recordingsWindow`, `workspacePromptWindow`, `settingsWindow`), the `openWorkspaceNamePrompt()`/`refreshWorkspaceProfilesTray()` helpers, and `startAgentStatusPolling()` (all now fully superseded by the dashboard's own tabs, which poll their own HTTP endpoints directly from the renderer). Removed the now-dangling `preload.js` entry from `package.json`'s electron-builder `files` list.
- [x] Add a **navigation dropdown in the dashboard itself**, similar to the webapp's [HeaderDropper](/c:/Users/tanne/Documents/Github/portfolio-app/frontend/src/components/HeaderDropper/HeaderDropper.jsx) hamburger menu, plus a prominent "Open Web App" action — requested directly after the first tab-by-tab pass shipped. A `#topbar` was added above the sidebar+content area with a ☰ toggle that opens a dropdown listing all 7 tabs (for muscle-memory parity with the webapp's own nav pattern) and a "Open Web App ↗" entry, alongside an always-visible "Open Web App ↗" button in the top bar itself for one-click access without opening the dropdown at all. The existing left sidebar tab list stays as the primary, always-visible navigation (it already satisfied "listed tabs to get to each page"); the dropdown supplements it rather than replacing it. `dashboard.html` now also reads `?tab=` from its own URL on load so a caller (tray, or a future deep link) can open it directly to a specific section.
- [x] No backend/API changes needed — all existing local HTTP endpoints (`/api/agent/*`, `/api/recorder/*`, `/api/skill/*`, `/api/automation/permissions*`, `/api/workspace-profiles*`, `/api/update/*`, the workspace-profiles module) stay exactly as they are; this phase is a pure UI consolidation. **Confirmed**: every tab reused an existing route or, where none existed (Status, Eye Tracking, "Open Web App"), added main-process IPC rather than new server routes, since that state was already main-process-only.
- [x] Keep tray notifications (`trayManager.notify(...)`) for background/async events (recording stopped, update ready, agent finished) even after the dashboard exists — the dashboard is an on-demand window, not always open, so passive tray notifications remain the right mechanism for "something happened while you weren't looking." **Confirmed unchanged**: `auto-updater.js`'s `update-downloaded` notification and all other existing `trayManager.notify(...)` call sites inside the automation code paths (workspace update/delete, etc.) are untouched by this cleanup.

### Explicitly out of scope
- No changes to the webapp ([frontend/](/c:/Users/tanne/Documents/Github/portfolio-app/frontend)) — this phase is 100% addon-side.
- No new inter-process framework, bundler, or UI component library — stay with plain HTML/CSS/JS to keep the addon's footprint and build small, per the guiding principles above.
- **Not blocked by Phase 0.** Like Phase 2, this is addon UX/quality-of-life work independent of whether the core perceive→act loop is fully reliable yet — it makes existing (and future-fixed) functionality easier to reach, but doesn't itself claim the core works, so it can proceed in parallel with Phase 0.

**Status: Phase 6 is functionally complete** (all tabs implemented, tray trimmed, legacy popups deleted, nav dropdown + one-click web-app access added) but still needs a real manual pass in a running Electron session — this environment can syntax-check the renderer/IPC code and run the existing automated test suite (31/31 passing, all pre-existing tests, none touch Electron UI directly) but can't click through a live GUI. Re-test each tab end-to-end against a real local server before considering this fully done.

**Update (2026-08-20 status check):** the Recorder & Skills tab specifically has kept getting real iteration since this was written — a full UI redesign, a live step-execution console, an NL macro compiler, and skill-list sync all shipped between 2026-08-08 and 2026-08-13 across addon builds #13–#27 (see `simple-addon/package.json` version history and the `agents/improve-addon-recorder-skills-tab` / `agents/dashboard-skills-edit-buttons-update` branches). That's a reasonable signal the dashboard has seen *some* real-world use since this status line was written, but it isn't the same as a deliberate, checked-off manual verification pass across all 7 tabs — treat this line as still open until that pass is actually done and recorded here.

### Real-world gap found and fixed after shipping: same version number republished, so "up to date" lied

After Phase 6 shipped and a release was tagged and published, a manual check reported "app is up to date" — but the running install genuinely didn't have the new dashboard code. Root cause: [package.json](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/package.json)'s version had been bumped to `1.0.9` well before any `addon-v1.0.9` tag was ever pushed, so an earlier (unpublished) build already had `1.0.9` baked in. When the dashboard work was finally released under that same, already-used version number, `electron-updater`'s semver-only comparison correctly found no difference — even though the code was actually different. Fixed by publishing a genuinely new version (`1.0.10`) — but that only fixes this one instance, not the underlying process gap. Two safeguards were added so this can't recur silently:

- [x] **CI guard**: a new "Guard against re-publishing an existing version" step in [build-addon.yml](/c:/Users/tanne/Documents/Github/portfolio-app/.github/workflows/build-addon.yml) checks (via `gh release view v<version>`) whether a GitHub Release already exists for the current `package.json` version before publishing, and fails the workflow with a clear error if so — forcing a real version bump before a new `addon-v*` tag can successfully publish.
- [x] **Runtime safety net**: a new [scripts/write-build-info.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/scripts/write-build-info.js) stamps every build with a real UTC `builtAt` timestamp into `build-info.json` (bundled into the packaged app via `package.json`'s `files` list, wired to run before every `electron-builder` invocation — both the CI workflow and the local `npm run build*` scripts via `prebuild`/`prebuild:win`/`prebuild:portable` hooks). [auto-updater.js](/c:/Users/tanne/Documents/Github/portfolio-app/simple-addon/auto-updater.js) now compares the checked release's own `releaseDate` (already provided by `electron-updater`'s `UpdateInfo`) against this build's own `builtAt` on every `update-not-available` event; if the release is newer despite matching version numbers, it sets a `possibleStaleVersion` flag (logged via `electron-log`, exposed through `/api/update/status`) rather than silently reporting a clean "up to date". The Dashboard's Updates tab (`dashboard.html`) shows a plain warning banner with a link to the GitHub Releases page when this flag is set, so a user isn't left thinking they're current when they're not.
- [x] **Fixed false-positive triggering on every up-to-date check**: the initial version of this runtime safety net compared `releaseDate` (stamped by `electron-builder` *during* packaging) directly against `builtAt` (stamped by `write-build-info.js` as a *prebuild* step, before packaging runs) with no tolerance — since packaging always finishes a few minutes after the prebuild step in the very same CI run, `releaseDate` was unavoidably later than `builtAt` for every single build, so `possibleStaleVersion` fired constantly, even right after a fresh, correctly-versioned install. `_checkForStaleVersion()` now only flags a gap larger than a 1-hour grace period (`STALE_VERSION_GRACE_MS`), which comfortably absorbs a normal CI build's packaging time while still catching a genuine same-version republish (which would happen well after the original build).
- This is a genuine belt-and-suspenders pair: the CI guard should make the mistake structurally impossible to publish again, and the runtime check catches it anyway if a release is ever created outside that workflow (e.g. a manual GitHub Release) or if the guard itself has a bug.

## Suggested order of execution

0. **Phase 0 comes first, full stop.** Purchasing and public Simple visibility stay off/caveated until its readiness bar is met — nothing else in this list matters to a customer if the core doesn't work.
1. Phase 1 (plain description) and Phase 3 (payment page accuracy) can be done in parallel once Phase 0 is resolved — both are copy/UX changes with no new backend work beyond usage-display (Phase 3's first item). Small, low-effort pieces can happen opportunistically even before Phase 0 finishes.
2. Phase 2 (install/update streamlining) and Phase 6 (lightweight in-addon dashboard) can both start any time — they're independent of the website copy work and of each other. Phase 2 arguably matters most for word-of-mouth, since a referred friend's first impression depends on it directly; Phase 6 makes the addon's existing (and future-fixed) capabilities easier to actually use day-to-day. Note both are still downstream of Phase 0 in effect: a smooth install of, or a nice dashboard for, a broken core isn't much of a win yet.
3. Phase 4 (ongoing reliability validation) runs continuously after Phase 0's bar is met and purchasing is re-enabled — its findings should keep the Phase 1 description honest as real usage accumulates.
4. Phase 5 (funnel honesty) comes after 1, 3, and 4 have shipped, so any price check happens against accurate, settled copy.

## What "done" looks like

- Purchasing and public Simple mentions are off (or plainly caveated as early/unfinished) until the Phase 0 readiness bar is genuinely met — nothing is sold that doesn't work.
- Key simulation, the perception loop, and auto-execution all work reliably for at least a defined set of real scenarios, with a known, honest success rate behind that claim.
- Purchase confirmation emails and the forgot/reset password flow are both manually confirmed working end-to-end — a paying customer gets proof of purchase and can always recover their own account.
- A new visitor can tell what Simple does from a plain, factual description — no hype required, no confusion either.
- Downloading, installing, and staying up to date takes as few manual steps as possible, and each remaining step is explained in plain language exactly when it's needed.
- The addon has one lightweight, tabbed dashboard window for its local-machine controls (agent, recorder/skills, workspace profiles, permissions, eye tracking, updates, status) instead of a sprawling tray right-click menu and scattered single-purpose popups — and it still supplements, not replaces, the webapp for anything account/cloud-related.
- Free vs. Pro differ **only** on things that cost real money or time to provide (storage, phone relay, support) — nothing is gated just to create an upgrade incentive.
- A user considering Pro can see their own real usage and a clear, honest statement of what changes with the upgrade — informational, not persuasive.
- You have a documented, honest reliability rate for the core automation loop, and nothing on the site claims more than that.
- Growth continues to come primarily from people who tried it and told others, not from marketing pressure on the site itself.
