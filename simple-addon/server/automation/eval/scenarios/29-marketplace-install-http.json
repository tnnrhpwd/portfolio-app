{
  "id": "29-marketplace-install-http",
  "name": "Marketplace install — POST /api/market/skills/:marketId/install (offline stub)",
  "description": "Exercises the addon's marketplace install proxy route end-to-end (docs/implementation/simple-agent-prompt.md §4.5), offline via the X-Simple-Eval-Stub header. Asserts the server-side downloads/installs counter shape (incremented atomically on the real backend — mirrored deterministically here). Part 4 of the marketplace chain — depends on 26-marketplace-publish-http having published mk-eval-001 in the same eval process.",
  "http": {
    "method": "POST",
    "path": "/api/market/skills/mk-eval-001/install",
    "headers": { "X-Simple-Eval-Stub": "1" },
    "body": { "version": "1.0.0" }
  },
  "expect": {
    "status": 200,
    "ok": true,
    "marketId": { "equals": "mk-eval-001" },
    "downloads": { "equals": 1 },
    "installs": { "equals": 1 },
    "steps": { "minLength": 1 }
  },
  "tags": ["marketplace", "http", "offline", "smoke"]
}
